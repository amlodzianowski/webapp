upstream app {
    server app.adrian-training.aws.logicworks.net:18000;
}
server {
        listen 80;

        location / {
            root   /data;
            index  index.html;
        }

        location /youtube {
                uwsgi_pass app;
        }
}
